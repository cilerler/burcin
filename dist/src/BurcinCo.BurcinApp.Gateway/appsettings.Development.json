{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      "Yarp": "Debug"
    }
  },
  "Gateway": {
    "WebhookAuth": {
      // Local-dev convenience: disable the shared-secret check. NEVER set Required=false outside of development.
      "Required": false
    }
  }
}
