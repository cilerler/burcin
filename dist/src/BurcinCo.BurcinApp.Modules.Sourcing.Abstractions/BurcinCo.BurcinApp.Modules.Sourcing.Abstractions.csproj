<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net10.0</TargetFramework>
	</PropertyGroup>

	<!-- Module's public cross-module contract. Dep-free by design — consumers reference only this csproj,
	     never the Modules.Sourcing implementation csproj. The public command/query surface and the inbound
	     IngredientQuoteResponseReceivedEvent wire contract live here. The service-internal Outbox request
	     event remains at the IngredientSupply service boundary instead of being exposed module-wide. -->

</Project>
