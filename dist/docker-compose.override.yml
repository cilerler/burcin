version: '3.4'

services:
#--if (ConsoleApplicationExists)
  burcin.console:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
#--endif
#--if (WebApiApplicationExists)
  burcin.api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"
#--endif
